<UserControl x:Class="Bastion.App.Pages.HomePage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ui="clr-namespace:Bastion.Ui.Controls;assembly=Bastion.Ui"
             xmlns:loc="clr-namespace:Bastion.Ui.Localization;assembly=Bastion.Ui"
             xmlns:conv="clr-namespace:Bastion.App.Converters"
             FontFamily="{DynamicResource Font.Ui}">
    <UserControl.Resources>
        <conv:ColorKeyToBrush x:Key="ColorKey" />
        <conv:BoolToVisibility x:Key="Bool2Vis" />
    </UserControl.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="26,4,26,24">
        <StackPanel MaxWidth="720" HorizontalAlignment="Left">

            <!-- Hero -->
            <Border Style="{DynamicResource Card}" Padding="24">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0" VerticalAlignment="Center">
                        <TextBlock x:Name="HeroCount" Style="{DynamicResource Text.Display}" Text="0 apps protected" />
                        <TextBlock x:Name="HeroSubtitle" Style="{DynamicResource Text.Secondary}" Margin="0,6,0,0"
                                   Text="Choose an app and it won't open without your password." />
                    </StackPanel>
                    <Button Grid.Column="1" x:Name="ProtectButton" Style="{DynamicResource Btn.Primary}" Height="44"
                            VerticalAlignment="Center" Click="Protect_Click">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="&#xE710;" FontFamily="Segoe Fluent Icons, Segoe MDL2 Assets" FontSize="15" VerticalAlignment="Center" Margin="0,0,9,0" />
                            <TextBlock Text="{loc:T Home_Protect}" VerticalAlignment="Center" />
                        </StackPanel>
                    </Button>
                </Grid>
            </Border>

            <!-- Protected apps -->
            <Grid Margin="2,26,0,10">
                <TextBlock Text="{loc:T Home_ProtectedApps}" Style="{DynamicResource Text.Subtitle}" />
                <Button Content="{loc:T Common_Manage}" Style="{DynamicResource Btn.Ghost}" Height="30" HorizontalAlignment="Right" Click="Manage_Click" />
            </Grid>

            <Border x:Name="EmptyState" Style="{DynamicResource Card}" Padding="28" Visibility="Collapsed">
                <StackPanel HorizontalAlignment="Center">
                    <Border Width="54" Height="54" CornerRadius="15" Background="{DynamicResource Brush.AccentSoft}">
                        <ui:BrandMark Width="30" Height="30" />
                    </Border>
                    <TextBlock Text="{loc:T Home_EmptyTitle}" Style="{DynamicResource Text.Subtitle}" HorizontalAlignment="Center" Margin="0,14,0,0" />
                    <TextBlock Text="{loc:T Home_EmptySub}" Style="{DynamicResource Text.Secondary}"
                               HorizontalAlignment="Center" TextAlignment="Center" Margin="0,4,0,0" />
                </StackPanel>
            </Border>

            <ItemsControl x:Name="AppsList">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Style="{DynamicResource Card}" Padding="14" Margin="0,0,0,10">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <Border Grid.Column="0" Width="42" Height="42" CornerRadius="11" Background="{DynamicResource Brush.SurfaceAlt}">
                                    <Grid>
                                        <Image Source="{Binding Icon}" Width="26" Height="26" Visibility="{Binding HasIcon, Converter={StaticResource Bool2Vis}}" RenderOptions.BitmapScalingMode="HighQuality" />
                                        <TextBlock Text="&#xECAA;" FontFamily="Segoe Fluent Icons, Segoe MDL2 Assets" FontSize="18"
                                                   Foreground="{DynamicResource Brush.TextTertiary}" HorizontalAlignment="Center" VerticalAlignment="Center"
                                                   Visibility="{Binding HasIcon, Converter={StaticResource Bool2Vis}, ConverterParameter=invert}" />
                                    </Grid>
                                </Border>
                                <StackPanel Grid.Column="1" Margin="14,0,0,0" VerticalAlignment="Center">
                                    <TextBlock Text="{Binding DisplayName}" Style="{DynamicResource Text.Subtitle}" FontSize="14" />
                                    <TextBlock Text="{Binding PolicyText}" Style="{DynamicResource Text.Caption}" Margin="0,2,0,0" />
                                </StackPanel>
                                <Border Grid.Column="2" VerticalAlignment="Center" CornerRadius="6" Padding="9,4"
                                        Background="{DynamicResource Brush.SurfaceAlt}">
                                    <TextBlock Text="{Binding StatusText}" Style="{DynamicResource Text.Caption}" />
                                </Border>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

            <!-- Recent activity -->
            <TextBlock Text="{loc:T Home_Recent}" Style="{DynamicResource Text.Subtitle}" Margin="2,20,0,10" />
            <Border Style="{DynamicResource Card}" Padding="6">
                <StackPanel>
                    <ItemsControl x:Name="ActivityList">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Grid Margin="12,10">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Grid.Column="0" Text="{Binding Glyph}" FontFamily="Segoe Fluent Icons, Segoe MDL2 Assets"
                                               FontSize="15" VerticalAlignment="Center" Width="22"
                                               Foreground="{Binding ColorKey, Converter={StaticResource ColorKey}}" />
                                    <TextBlock Grid.Column="1" Text="{Binding Title}" Style="{DynamicResource Text.Body}" FontSize="13" VerticalAlignment="Center" Margin="6,0,0,0" />
                                    <TextBlock Grid.Column="2" Text="{Binding TimeText}" Style="{DynamicResource Text.Caption}" VerticalAlignment="Center" />
                                </Grid>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                    <TextBlock x:Name="NoActivity" Text="{loc:T Home_NoActivity}" Style="{DynamicResource Text.Secondary}" Margin="14,12" Visibility="Collapsed" />
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</UserControl>
